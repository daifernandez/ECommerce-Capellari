"use client";

export default function SearchComponent() {
  const handleSearch = (term) => {
    // Mover la lógica de búsqueda aquí
  };

  return (
    <div>
      {/* Usar handleSearch directamente aquí */}
    </div>
  );
} 
